name: progress_circle
description: ProgressCircle is an easy widget to use for creating some circle progress tracker.
version: 1.1.0
homepage: https://github.com/Rollfreeze/progress_circle

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
